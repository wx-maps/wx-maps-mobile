import {HIDE_SNACKBAR, SHOW_SNACKBAR} from '../actions/AppActions'

const INITIAL_STATE = {
    lightsOn: true,
};

export const ControlReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
      default:
        return state
    }
  };